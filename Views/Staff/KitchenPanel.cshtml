@model IEnumerable<projet_info_finale.Models.Order>

@{
    ViewData["Title"] = "Kitchen Staff Panel";
}

<div class="d-flex justify-content-center align-items-center min-vh-100">
    <div class="glass-card w-75">
        <h2 class="text-center mb-4 text-gradient">Kitchen Staff Panel</h2>
        
        <div class="orders-container">
            @foreach (var order in Model)
            {
                <div class="order-card mb-3">
                    <!-- Order header -->
                    <div class="d-flex justify-content-between align-items-center">
                        <h5 class="text-light">Order #@order.OrderID</h5>
                        <span class="badge @(order.OrderStatus == "Pending" ? "bg-warning" : 
                                           order.OrderStatus == "Preparing" ? "bg-info" : 
                                           "bg-success")">
                            @order.OrderStatus
                        </span>
                    </div>

                    <!-- Order details -->
                    <div class="order-details">
                        <p class="text-light mb-2">Items:</p>
                        @foreach (var item in order.OrderItems)
                        {
                            <p class="text-light ms-3">@item.Quantity x @item.MenuItem.ItemName</p>
                        }
                        <p class="text-light mb-2">Delivery Details:</p>
                        <p class="text-light ms-3">@order.User.Address</p>
                        <p class="text-light ms-3">@order.User.City, @order.User.PostalCode</p>
                        
                        @if (order.OrderStatus == "Pending")
                        {
                            <form method="post" asp-controller="Staff" asp-action="UpdateOrderStatus" class="mt-3">
                                <input type="hidden" name="orderId" value="@order.OrderID" />
                                <input type="hidden" name="status" value="Preparing" />
                                <button type="submit" class="btn btn-gradient">Start Preparing</button>
                            </form>
                        }
                        else if (order.OrderStatus == "Preparing")
                        {
                            <form method="post" asp-controller="Staff" asp-action="UpdateOrderStatus" class="mt-3">
                                <input type="hidden" name="orderId" value="@order.OrderID" />
                                <input type="hidden" name="status" value="Out for Delivery" />
                                <button type="submit" class="btn btn-gradient">Mark Ready for Delivery</button>
                            </form>
                        }
                    </div>
                </div>
            }
        </div>
    </div>
</div>


<style>
    body {
        background: #121212;  /* Just use the solid color */
        font-family: 'Poppins', sans-serif;
        margin: 0;
        height: 100vh;
        color: white;
    }

    .text-gradient {
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .glass-card {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(15px);
        padding: 30px;
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
    }

    .order-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        padding: 20px;
        border-radius: 15px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 20px;
    }

    .btn-gradient {
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        color: white;
        font-weight: 600;
        border: none;
        border-radius: 30px;
        padding: 10px 25px;
        transition: transform 0.3s, box-shadow 0.3s;
    }

    .btn-gradient:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(255, 126, 95, 0.6);
        color: white;
    }

    .badge {
        padding: 8px 16px;
        border-radius: 20px;
        font-weight: 500;
    }
</style>
